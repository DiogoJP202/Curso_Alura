alert("Boas vindas ao nosso site!"); // Mostre um alerta com a mensagem "Boas vindas ao nosso site!".

alert("Erro! Preencha todos os campos"); // Exiba um alerta com o texto "Erro! Preencha todos os campos"

const userName = prompt("Qual é o seu nome? :"); // Use um prompt para perguntar o nome do usuário e armazená-lo na variável nome.

const userAge = prompt("Qual é a sua idade? :"); // Peça ao usuário para digitar sua idade usando um prompt e armazene-a na variável idade.

userAge >= 18 ? alert(`${userName}, você já pode tirar a habilitação!`) : alert(`${userName}, você ainda não pode tirar a habilitação!`);

const name = "lua"; // Declare uma variável chamada nome e atribua a ela o valor "Lua".

const idade = 25; // Crie uma variável chamada idade e atribua a ela o valor 25.

const numeroDeVendas = 50; // Defina uma variável numeroDeVendas e atribua a ela o valor 50.

const saldoDisponivel = 1000; // Defina uma variável saldoDisponivel e atribua a ela o valor 1000.

const mensagemDeErro = "Erro! Preencha todos os campos"; //Declare uma variável chamada mensagemDeErro e atribua a ela o valor "Erro! Preencha todos os campos" Agora exiba um alerta com o valor da variável mensagemDeErro.